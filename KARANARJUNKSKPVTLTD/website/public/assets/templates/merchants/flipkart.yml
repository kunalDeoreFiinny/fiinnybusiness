id: flipkart_order_txn_v1
applies_if:
  sender_any: ['FLPKRT', 'FLIPKART', 'no-reply']
  contains_any: ['order', 'refund', 'inr']
extract:
  amount: { regex: "(?:inr|₹|rs\\.?)+\\s*([0-9,]+\\.?[0-9]{0,2})" }
  merchant: { regex: "(flipkart)" }
classify:
  direction: "UNKNOWN"
  instrument: "UNKNOWN"
  confidence: "0.84"
post:
  category_hint: "Shopping"
