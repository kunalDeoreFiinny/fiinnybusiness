id: phonepe_upi_v1
applies_if:
  sender_any: ['PHONEPE', 'PPAY', 'phonepe']
  contains_any: ['upi', 'inr']
extract:
  amount: { regex: "(?:inr|₹|rs\\.?)+\\s*([0-9,]+\\.?[0-9]{0,2})" }
  merchant: { regex: "(?:to|from)\\s+([a-z0-9._@-]{3,50})" }
classify:
  direction: "UNKNOWN"   # will infer using 'to' vs 'from' in future pass
  instrument: "UPI"
  confidence: "0.90"
post:
  category_hint: "Transfers"
