id: amazonpay_wallet_v1
applies_if:
  sender_any: ['AMAZON', 'AMAZONPAY', 'AZPAY']
  contains_any: ['amazon pay', 'inr', 'paid', 'received']
extract:
  amount: { regex: "(?:inr|₹|rs\\.?)+\\s*([0-9,]+\\.?[0-9]{0,2})" }
  merchant: { regex: "(?:to|from|at)\\s+([a-z0-9 &._@-]{2,50})" }
classify:
  direction: "UNKNOWN"
  instrument: "WALLET"
  confidence: "0.87"
post:
  category_hint: "Shopping"
