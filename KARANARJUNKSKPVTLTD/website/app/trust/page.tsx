import React from "react";
import TrustPageContent from "./components/TrustPageContent";

export const metadata = {
    title: 'Trust & Security | Fiinny',
    description: 'Your financial data belongs to you. Fiinny uses on-device processing and bank-grade encryption to ensure your privacy is never compromised.',
};

export default function TrustPage() {
    return <TrustPageContent />;
}
